# クリフトンストレングス 177問診断ツール 仕様書

| 項目 | 内容 |
|------|------|
| バージョン | v1.0.0 |
| 作成日 | 2026年5月24日 |
| 更新日 | 2026年5月24日 |
| ファイル形式 | HTML（シングルファイル） |
| 公開URL | https://strengths-profile-jp.pages.dev |
| リポジトリ | https://github.com/az-tokyowork/strengths-profile-jp |

---

## 1. プロジェクト概要

クリフトンストレングスの34資質に基づいた177問の**対比式診断テスト**をWebブラウザ上で実施し、ユーザー固有の強みプロフィールをリアルタイムで可視化するHTMLアプリケーションです。サーバーレス・シングルファイルで動作し、インストール不要で利用できます。

### 1.1 開発背景

仕事上の強み・弱みを客観的に把握し、キャリア方向性や副業選択の意思決定に活用することを目的として開発しました。既存のストレングスファインダー®は有料サービスですが、本ツールは学習・自己理解のための参考ツールとして独自設計したものです。

### 1.2 主な特長

- **177問の対比式（バイポーラ）形式** — 2つの対照的な記述をA・B両端に配置し、1〜5のスケールでどちらに近いかを選択
- **34資質すべてを10問前後でスコアリング** — 各質問が2資質に同時にポイントを付与する効率的なアルゴリズム
- **回答後0.35秒で自動進行** — テンポよく直感的に答えられるUI設計
- **即時結果表示** — 全34資質ランキング・ドメイン別スコア・トップ5詳細を診断完了後すぐに可視化
- **外部依存ゼロ** — サーバー不要、APIキー不要、オフラインでも動作

---

## 2. インフラ・デプロイ構成

| 項目 | 内容 |
|------|------|
| ホスティング | Cloudflare Pages（無料プラン） |
| ビルド方式 | 静的サイト（ビルドコマンドなし） |
| デプロイトリガー | GitHub `main` ブランチへのプッシュで自動デプロイ |
| 認証 | なし（パブリック公開） |
| カスタムドメイン | 未設定（`*.pages.dev` サブドメインを使用） |

---

## 3. 機能要件

### 3.1 画面構成（3タブ）

| タブ名 | 機能概要 | 主要コンポーネント |
|--------|----------|-------------------|
| 📖 34資質一覧 | 34資質の説明をドメイン別に一覧表示。各資質をクリックすると詳細・活用ヒントが展開 | 4ドメイン × カード、アコーディオン展開 |
| 📝 177問診断 | 対比式177問の診断テストを実施。進捗バー・自動進行・前後ナビゲーション付き | プログレスバー、バイポーラ問題カード、5段階スケール |
| 📊 結果分析 | 診断完了後に34資質ランキング・上位5資質詳細・ドメイン別スコアを表示 | ランキング表、スコアバー、詳細カード |

### 3.2 177問診断機能 詳細

#### 回答方式

各質問は「文章A（左）」と「文章B（右）」の2つの対照的な記述で構成されます。ユーザーは1〜5の5段階スケールでどちらにより近いかを選択します。

| スケール | 意味 |
|----------|------|
| **1** | 完全にAに近い |
| **2** | ややAに近い |
| **3** | どちらでもない |
| **4** | ややBに近い |
| **5** | 完全にBに近い |

#### 自動進行

回答選択から **350ミリ秒後** に次の質問へ自動遷移します。「前の質問」「次の質問」ボタンによる手動ナビゲーションも可能です。

#### 進捗表示

画面上部のプログレスバーに「回答済み問数 / 177問」とパーセンテージをリアルタイム表示します。

#### 完了処理

177問すべての回答完了後、完了パネルが表示されます。「結果を見る」ボタンで結果分析タブへ遷移し、スコアリングと可視化を実行します。

---

## 4. データ設計

### 4.1 資質マスタデータ構造

34資質を以下のオブジェクト形式で定義します（計34件）。

```js
{
  id:   "achiever",        // 資質ID（英字）
  name: "達成欲",          // 日本語名
  en:   "Achiever",        // 英語名
  cat:  "exe",             // ドメイン: exe / inf / rel / str
  desc: "説明テキスト",    // 資質の説明（1〜2文）
  tips: ["ヒント1", ...]   // 活用のヒント（配列・3項目）
}
```

### 4.2 ドメイン区分（4区分）

| ドメイン | ID | カラー | 含まれる資質 | 資質数 |
|----------|----|--------|-------------|--------|
| 実行力 | `exe` | `#7A32A3`（紫） | 達成欲・アレンジ・信念・公平性・慎重さ・規律性・目標志向・責任感・回復志向 | 9 |
| 影響力 | `inf` | `#E87722`（橙） | 活発性・指令性・コミュニケーション・競争性・最上志向・自己確信・自我・社交性 | 8 |
| 人間関係構築力 | `rel` | `#1B6BAE`（青） | 適応性・運命思考・成長促進・共感性・調和性・包含・個別化・ポジティブ・親密性 | 9 |
| 戦略的思考力 | `str` | `#00855B`（緑） | 分析思考・原点思考・未来志向・着想・収集心・内省・学習欲・戦略性 | 8 |

### 4.3 質問データ構造（177問）

各質問は以下の形式で定義され、2つの資質に対し同時にスコアを付与します。

```js
{
  l: "achiever",        // A側（左）に対応する資質ID
  r: "adaptability",    // B側（右）に対応する資質ID
  a: "毎日、何かを…",  // A側の文章
  b: "計画よりも…"     // B側の文章
}
```

### 4.4 質問数の内訳

```
34資質 × 5問（各資質がA側に登場） = 170問
補完クロスドメイン問題              =   7問
────────────────────────────────────────────
合計                                = 177問
```

各資質はA側・B側の両方を通じて平均 **10〜11問** に登場し、十分な測定精度を確保します。

---

## 5. スコアリングアルゴリズム

### 5.1 スコア計算方式

回答値 `k`（1〜5）に対し、各資質へのポイント付与は以下の通りです。

| ポジション | 計算式 | k=1 | k=2 | k=3 | k=4 | k=5 |
|-----------|--------|-----|-----|-----|-----|-----|
| A側（左）の資質 | `raw += (6 - k)` | 5点 | 4点 | 3点 | 2点 | 1点 |
| B側（右）の資質 | `raw += k` | 1点 | 2点 | 3点 | 4点 | 5点 |

### 5.2 正規化（0〜100点）

```
最終スコア = raw_scoreの合計 ÷ (登場問数 × 5) × 100
```

登場問数の多少に関わらず公平な比較が可能です。

### 5.3 ドメインスコア

各ドメインに属する資質スコアの**平均値**をドメインスコアとして算出します。最高ドメインスコアをそのユーザーの「支配的ドメイン」として結果画面の冒頭に表示します。

---

## 6. UI/UX設計

### 6.1 カラーシステム

| ドメイン | カラー | HEX |
|----------|--------|-----|
| 実行力（exe） | 紫 | `#7A32A3` |
| 影響力（inf） | 橙 | `#E87722` |
| 人間関係構築力（rel） | 青 | `#1B6BAE` |
| 戦略的思考力（str） | 緑 | `#00855B` |

各ドメインカラーは、資質カード上部帯・バッジ・スコアバー・見出しラインに一貫して使用します。

### 6.2 バイポーラ問題カードのレイアウト

**デスクトップ（641px以上）**

```
┌─────────────────────────────────────────────────────────┐
│ [A 文章テキスト]   [1][2][3][4][5]   [B 文章テキスト]  │
│         ← Aに近い              Bに近い →               │
└─────────────────────────────────────────────────────────┘
```

**モバイル（640px以下）**

```
┌──────────────────────┐
│ A: 文章テキスト      │
│ [1] [2] [3] [4] [5]  │
│ B: 文章テキスト      │
└──────────────────────┘
```

### 6.3 レスポンシブ対応

| ブレークポイント | レイアウト変更 |
|-----------------|---------------|
| 641px以上（デスクトップ） | バイポーラ問題：横3カラム / 結果グリッド：2カラム |
| 640px以下（モバイル） | バイポーラ問題：縦積み / 結果グリッド：1カラム |

### 6.4 アニメーション仕様

| 要素 | 仕様 |
|------|------|
| プログレスバー | 回答ごとにリアルタイム更新（`transition: width 0.4s ease`） |
| スコアバー | 結果表示時にアニメーション描画（`transition: width 1.2s ease`） |
| 自動進行 | 回答選択350ms後に次問へ遷移（`opacity 0.2s`） |
| スケールボタン | ホバー時に `scale(1.08)`、選択時に `scale(1.12)` |

---

## 7. 技術仕様

| 項目 | 内容 |
|------|------|
| 実装言語 | HTML5 / CSS3 / Vanilla JavaScript（ES6+） |
| ファイル名 | `index.html`（シングルファイル） |
| 外部依存 | なし（ライブラリ・CDN・API 一切不使用） |
| データ保存 | インメモリのみ（localStorage未使用） |
| 推奨ブラウザ | Chrome 100+ / Safari 15+ / Firefox 100+ / Edge 100+ |

### 7.1 JavaScriptモジュール構成

| 関数・定数 | 役割 |
|-----------|------|
| `const CATS` | ドメイン（exe/inf/rel/str）のメタデータ定義 |
| `const S` | 34資質マスタデータ配列 |
| `const Q` | 177問の質問データ配列 |
| `buildBrowse()` | 34資質一覧タブのHTML生成 |
| `startQuiz()` | 診断開始・初期化処理 |
| `answer(val)` | 回答記録・進捗更新・自動進行制御 |
| `prevQ() / nextQ()` | 前後の質問へのナビゲーション |
| `calcScores()` | スコアリングアルゴリズム実行・34資質スコア算出 |
| `showResults()` | 結果分析タブのHTML生成・アニメーション起動 |
| `resetQuiz()` | 診断リセット・初期状態への復元 |
| `showTab(id)` | タブ切り替え処理 |

---

## 8. 画面フロー

```
[アプリ起動]
     │
     ├─ 34資質一覧タブ ──→ 各資質クリック ──→ 詳細展開（アコーディオン）
     │
     ├─ 177問診断タブ
     │       │
     │       ├─ 診断開始ボタン
     │       │       │
     │       │       └─ 質問1〜177（バイポーラ形式）
     │       │               │ 回答選択
     │       │               │ 350ms後自動進行
     │       │               │
     │       │           全177問完了
     │       │               │
     │       │           完了パネル表示
     │       │               │
     │       └─ 「結果を見る」ボタン
     │
     └─ 結果分析タブ
             │
             ├─ 支配的ドメイン表示
             ├─ ドメイン別スコアバー
             ├─ 上位5資質 詳細カード
             ├─ 34資質全体ランキング
             └─ やり直しボタン ──→ 診断タブへ戻る
```

---

## 9. 制限事項・注意事項

> ⚠️ 本ツールはクリフトンストレングス®の公式ツールではなく、参考・学習目的の独自実装です。

- 質問文・採点ロジックはGallup社の公式テストとは異なります。結果は公式ストレングスファインダー®の診断結果と一致しない場合があります
- 回答データはブラウザのメモリ上にのみ保存されます。ページを閉じると回答・結果は消去されます
- スコアは絶対的な評価値ではなく、相対的な傾向の目安として参照してください
- 診断時間は個人差がありますが、通常20〜30分かかります
